import React from "react";
import styled from "styled-components";

const Profile = () => {
  return <Wrapper>hiiii this is profile section</Wrapper>;
};
const Wrapper = styled.section`
padding:10rem 0;`
export default Profile;
